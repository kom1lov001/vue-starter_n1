// tailwind.config.ts
import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#4CAF4F",
        secondary: "#9333EA",
        accent: "#FBBF24",
        dark: "#1F2937",
      },
    },
  },
  plugins: [],
};

export default config;
